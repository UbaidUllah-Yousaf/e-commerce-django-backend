# Copy to your Vite app root as `.env` or `.env.local`

# Django API (no trailing slash)
VITE_API_URL=http://localhost:8000

# Optional: only if you build redirect URLs on the client instead of using
# GET /api/v1/stripe/config/ → payment_options.effective_checkout_urls
# VITE_STOREFRONT_URL=http://localhost:5173
